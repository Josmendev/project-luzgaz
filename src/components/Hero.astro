---
import ArrowDownToDot from "./icon/ArrowDownToDot.astro";
import Button from "./ui/Button.astro";
---

<section
  id="inicio"
  class="relative flex items-center justify-center overflow-hidden"
  style="min-height: 100vh; padding-top: 80px;"
>
  <!-- Background Image -->
  <div
    class="absolute inset-0 z-0"
    style="background-image: url('/inicio-1.webp'); background-size: cover; background-position: center; background-repeat: no-repeat;"
  >
    <!-- Dark overlay for better text readability -->
    <div class="absolute inset-0 bg-black/40"></div>
  </div>

  <!-- Content -->
  <div
    class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 text-center flex flex-col items-center justify-center h-full"
  >
    <div class="animate-slide-up">
      <!-- Main Title -->
      <h1
        class="text-4xl sm:text-5xl lg:text-7xl font-bold text-white leading-tight drop-shadow-lg mb-8!"
      >
        Transparencia total
        <span class="block text-luzgas-green -mt-2">Ahorro real</span>
      </h1>

      <!-- Description -->
      <p
        class="text-xl sm:text-2xl text-white/90 mb-12! max-w-4xl mx-auto leading-relaxed drop-shadow-md font-medium mt-12"
      >
        Analizamos tus facturas GRATIS. Comparamos todas las comercializadoras del mercado 
    y te decimos exactamente cuánto puedes ahorrar. Asesoramiento 100% independiente.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
        <Button
          variant="gradient"
          color="primary"
          dimension="xl"
          class="min-w-[280px] backdrop-blur-sm !text-xl font-medium!"
        >
          <a href="/invoice" class="block w-full h-full">
            Solicitar Estudio Gratuito
          </a>
        </Button>

        <Button
          variant="outline"
          color="secondary"
          dimension="xl"
          class="min-w-[280px] backdrop-blur-sm !text-xl font-medium! border-white text-white bg-white/10 hover:bg-white hover:text-secondary-500"
        >
          <a href="/our-services" class="block w-full h-full"> Contáctanos </a>
        </Button>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <a
    href="#featured-services"
    class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 hover:scale-110 transition-transform duration-300"
  >
    <div class="animate-bounce">
      <ArrowDownToDot class="w-8 h-8 text-white" />
    </div>
  </a>
</section>
